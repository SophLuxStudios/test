using UnityEngine;

namespace ActionSystem
{
    public class TackleAction : IAction
    {
        private readonly Character _target;

        public TackleAction(Character target)
        {
            _target = target;
        }

        public void Execute(Character character)
        {
            Debug.Log($"{character.name} tackled {_target.name}!");
            
            // Add tackle logic//////////////////////////////////////////////////////////////////////////
        }
    }
}
